import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Hides the in-browser Next.js dev tools indicator (corner “N” / next control). */
  devIndicators: false,
};

export default nextConfig;
